{
  "name": "tuition-aspirasi-murni",
  "version": "1.0.0",
  "description": "Tuition Centre Aspirasi Murni Fee Payment System",
  "scripts": {
    "": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": [
    "tuition",
    "payment",
    "management",
    "react",
    "nodejs",
    "mongodb"
  ],
  "author": "Tuition Centre Aspirasi Murni",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "mongodb": "^6.18.0"
  }
}
